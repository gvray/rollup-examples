/**
 * 生成问候语
 * @param {string} name - 要问候的名称
 * @returns {string} 问候语
 */
export function sayHello(name) {
  return `Hello, ${name}!`;
}

/**
 * 计算两个数的和
 * @param {number} a - 第一个数
 * @param {number} b - 第二个数
 * @returns {number} 两数之和
 */
export function add(a, b) {
  return a + b;
}